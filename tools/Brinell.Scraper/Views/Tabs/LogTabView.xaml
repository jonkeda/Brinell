<UserControl x:Class="Brinell.Scraper.Views.Tabs.LogTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:Brinell.Scraper.Converters"
             xmlns:logging="clr-namespace:Microsoft.Extensions.Logging;assembly=Microsoft.Extensions.Logging.Abstractions">
    <UserControl.Resources>
        <converters:LogLevelToBrushConverter x:Key="LogLevelToBrush"/>
        <converters:LogLevelDisplayConverter x:Key="LogLevelDisplay"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility"/>
        <Style x:Key="LogRowStyle" TargetType="DataGridRow">
            <Style.Triggers>
                <DataTrigger Binding="{Binding Level}" Value="{x:Static logging:LogLevel.Warning}">
                    <Setter Property="Background" Value="#FFF8DC"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Level}" Value="{x:Static logging:LogLevel.Error}">
                    <Setter Property="Background" Value="#FFE5E5"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Level}" Value="{x:Static logging:LogLevel.Critical}">
                    <Setter Property="Background" Value="#FFCCCC"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <DockPanel>
        <!-- Top toolbar -->
        <Border DockPanel.Dock="Top" Background="#F5F5F5" BorderBrush="#DDD" BorderThickness="0,0,0,1" Padding="6,4">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="📋 Logs" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,16,0"/>
                <TextBlock Text="Level:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                <ComboBox ItemsSource="{Binding LogLevels}"
                          SelectedItem="{Binding SelectedLogLevel}"
                          Width="110" Margin="0,0,12,0">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Converter={StaticResource LogLevelDisplay}}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
                <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,4,0"/>
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Width="220" Margin="0,0,12,0" VerticalContentAlignment="Center"/>
                <Button Content="Clear" Command="{Binding ClearLogsCommand}" Padding="10,2" Margin="0,0,4,0"/>
                <Button Content="Export" Command="{Binding ExportCommand}" Padding="10,2" Margin="0,0,12,0"/>
                <CheckBox Content="Auto-scroll" IsChecked="{Binding IsAutoScroll}" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Bottom status bar -->
        <Border DockPanel.Dock="Bottom" Background="#F5F5F5" BorderBrush="#DDD" BorderThickness="0,1,0,0" Padding="6,3">
            <StackPanel Orientation="Horizontal">
                <TextBlock>
                    <Run Text="{Binding TotalCount, Mode=OneWay}"/>
                    <Run Text=" entries"/>
                </TextBlock>
                <TextBlock Text=" | " Foreground="#888" Margin="6,0"/>
                <TextBlock>
                    <Run Text="Showing: "/>
                    <Run Text="{Binding FilteredCount, Mode=OneWay}"/>
                </TextBlock>
                <TextBlock Text=" | " Foreground="#888" Margin="6,0"/>
                <TextBlock>
                    <Run Text="Auto-scroll: "/>
                    <Run Text="On">
                        <Run.Style>
                            <Style TargetType="Run">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsAutoScroll}" Value="False">
                                        <Setter Property="Text" Value="Off"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Run.Style>
                    </Run>
                </TextBlock>
            </StackPanel>
        </Border>

        <!-- DataGrid -->
        <DataGrid x:Name="LogGrid"
                  ItemsSource="{Binding FilteredLogEntries}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal"
                  RowHeaderWidth="0"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Extended"
                  SelectionUnit="FullRow"
                  RowStyle="{StaticResource LogRowStyle}"
                  ClipboardCopyMode="IncludeHeader"
                  EnableRowVirtualization="True"
                  EnableColumnVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  FontFamily="Consolas, Courier New"
                  FontSize="12">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Timestamp"
                                    Binding="{Binding Timestamp, StringFormat=yyyy-MM-dd HH:mm:ss.fff}"
                                    Width="160"/>
                <DataGridTextColumn Header="Level" Binding="{Binding Level}" Width="90">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{Binding Level, Converter={StaticResource LogLevelToBrush}}"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Source" Binding="{Binding Source}" Width="180"/>
                <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
