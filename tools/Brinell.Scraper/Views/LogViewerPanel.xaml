<UserControl x:Class="Brinell.Scraper.Views.LogViewerPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:Brinell.Scraper.Converters">
    <UserControl.Resources>
        <converters:LogLevelToBrushConverter x:Key="LogLevelToBrush"/>
        <converters:LogLevelDisplayConverter x:Key="LogLevelDisplay"/>
    </UserControl.Resources>
    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="4,2">
            <TextBlock Text="📋 Logs" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <TextBlock Text="Level:" VerticalAlignment="Center" Margin="0,0,4,0"/>
            <ComboBox ItemsSource="{Binding LogLevels}"
                      SelectedItem="{Binding SelectedLogLevel}"
                      Width="120" Margin="0,0,8,0">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Converter={StaticResource LogLevelDisplay}}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <Button Content="Clear" Command="{Binding ClearLogsCommand}" Padding="8,2"/>
        </StackPanel>
        <ListBox ItemsSource="{Binding FilteredLogEntries}"
                 x:Name="LogList"
                 ScrollViewer.CanContentScroll="True"
                 VirtualizingPanel.IsVirtualizing="True"
                 VirtualizingPanel.VirtualizationMode="Recycling"
                 BorderThickness="0"
                 FontFamily="Consolas, Courier New"
                 FontSize="12">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Timestamp, StringFormat=HH:mm:ss.fff}"
                                   Width="90" Foreground="Gray"/>
                        <TextBlock Text="{Binding Level}" Width="80"
                                   Foreground="{Binding Level, Converter={StaticResource LogLevelToBrush}}"/>
                        <TextBlock Text="{Binding Source}" Width="140" Foreground="DarkCyan"/>
                        <TextBlock Text="{Binding Message}" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </DockPanel>
</UserControl>
