<UserControl x:Class="Brinell.Scraper.Views.DomTreePanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:Brinell.Scraper.Models">
    <DockPanel>
        <!-- Filter bar -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="4">
            <TextBlock Text="🔎" VerticalAlignment="Center" Margin="0,0,4,0"/>
            <TextBox Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                     MinWidth="200" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- DOM Tree -->
        <TreeView ItemsSource="{Binding RootElements}"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  MouseLeftButtonUp="TreeView_MouseLeftButtonUp">
            <TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem">
                    <Setter Property="IsExpanded" 
                            Value="{Binding DataContext.IsFilterActive, 
                                    RelativeSource={RelativeSource AncestorType=TreeView}}"/>
                    <EventSetter Event="MouseEnter" Handler="TreeItem_MouseEnter"/>
                    <EventSetter Event="MouseLeave" Handler="TreeItem_MouseLeave"/>
                </Style>
            </TreeView.ItemContainerStyle>
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate DataType="{x:Type models:DomElement}"
                                          ItemsSource="{Binding Children}">
                    <StackPanel Orientation="Horizontal">
                        <CheckBox IsChecked="{Binding DataContext.IsSelected, RelativeSource={RelativeSource Self}}"
                                  Margin="0,0,4,0"
                                  Visibility="Collapsed"/>
                        <TextBlock>
                            <Run Text="&lt;" Foreground="Gray"/>
                            <Run Text="{Binding Tag, Mode=OneWay}" Foreground="#2196F3" FontWeight="SemiBold"/>
                            <Run Text="{Binding Id, Mode=OneWay, StringFormat=' id=&quot;{0}&quot;', TargetNullValue=''}" Foreground="#FF9800"/>
                            <Run Text="{Binding ClassName, Mode=OneWay, StringFormat=' class=&quot;{0}&quot;', TargetNullValue=''}" Foreground="#4CAF50"/>
                            <Run Text="&gt;" Foreground="Gray"/>
                        </TextBlock>
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </DockPanel>
</UserControl>
